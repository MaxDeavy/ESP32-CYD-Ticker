# Kopieren nach server/deploy.env und anpassen:
#   cp deploy.env.example deploy.env
#
# Nginx-Config erzeugen:
#   cd deploy && ./generate-nginx.sh

SERVER_NAME=deine-domain.de
HOST_PORT=4546
